<ul>
  <% blog.articles.each do |article| %>
    <li class="space-x-4">
      <time>[<%= article.date.strftime('%b %d %Y') %>]</time>
      <%= link_to article.title, article.url + "/" %>
    </li>
  <% end %>
</ul>
